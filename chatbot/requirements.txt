# Pinned, known-good versions verified to resolve as wheels (no source builds)
# for CPython 3.12 / win_amd64. Use a Python 3.12 virtual environment:
#   py -3.12 -m venv .venv   (install 3.12 first if needed)
# Native deps (faiss-cpu, fastembed -> onnxruntime/tokenizers/numpy) may not
# have wheels for very new Python versions like 3.14, so 3.12 is recommended.

# Web framework
fastapi==0.136.3
uvicorn[standard]==0.49.0

# LLM providers
anthropic==0.105.2          # production target (Claude)
groq==1.4.0                 # free dev option (OpenAI-compatible)

# Local embeddings (no API key, ONNX-based — no torch required)
fastembed==0.8.0

# Vector index
faiss-cpu==1.14.2

# Config / validation
pydantic==2.13.4
pydantic-settings==2.14.1

# Rate limiting
slowapi==0.1.9
